import { StyleSheet } from "react-native";

export const searchCep_styles = StyleSheet.create({
    map: {
        flex: 1,
        marginTop: 16,
        borderRadius: 8,
    },
    addressContainer: {
        padding: 28,
        backgroundColor: '#fff',
        borderRadius: 8,
        marginTop: 5,
        elevation: 2,
    },
    addressText: {
        fontSize: 14,
        color: '#333',
        marginBottom: 4,
    },
});